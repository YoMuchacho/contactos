import { FiltroContactoPipe } from './filtro-contacto.pipe';

describe('FiltroContactoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroContactoPipe();
    expect(pipe).toBeTruthy();
  });
});
